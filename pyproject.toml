[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "cosine"
version = "0.1.0"
description = "COSINE application package"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]